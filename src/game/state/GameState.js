export default class GameState {
  constructor(name, displaySplash) {
    this.name = name;
    this.displaySplash = displaySplash;
  }

  setPlayer(player) {
    // Only do something if defined
  }
}